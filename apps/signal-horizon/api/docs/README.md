# Signal Horizon API Docs (Dev)

This folder is a lightweight docs root used by the UI Support Hub in local development.

## Useful Links

- AUTH_PATTERNS: Authentication + scopes patterns (`AUTH_PATTERNS.md`)
- TYPESCRIPT_CODE_REVIEW: TS conventions (`TYPESCRIPT_CODE_REVIEW.md`)

## Notes

- Served via `GET /api/v1/docs` and `GET /api/v1/docs/:id`.
- Doc ids are path-based with `:` separators, e.g. `README`, `AUTH_PATTERNS`.

