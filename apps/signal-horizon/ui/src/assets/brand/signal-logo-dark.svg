<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<svg
   width="80"
   height="80.000008"
   viewBox="0 0 80 80.000008"
   version="1.1"
   id="svg184"
   xmlns="http://www.w3.org/2000/svg"
   xmlns:svg="http://www.w3.org/2000/svg">
  <defs
     id="defs184" />
  <!-- Dark Background Section -->
  <!-- Light Background Section -->
  <!-- ========== DARK SECTION ========== -->
  <!-- Horizontal Primary - 80px icon (0.8 scale) -->
  <!-- Horizontal with Tagline -->
  <!-- Stacked -->
  <!-- Stacked with Tagline -->
  <!-- Wordmark Only -->
  <!-- With Atlas Crew Endorsement - 60px icon (0.6 scale) -->
  <!-- Size Variations -->
  <!-- 64px (0.64 scale) -->
  <!-- 44px (0.44 scale) -->
  <!-- 28px (0.28 scale) -->
  <!-- 20px (0.2 scale) -->
  <!-- Fleet Status Badge - light blue border -->
  <!-- Compact Badge -->
  <!-- ========== LIGHT SECTION ========== -->
  <!-- Horizontal Primary Light - 80px icon -->
  <!-- Horizontal with Tagline Light -->
  <!-- Stacked Light -->
  <!-- Wordmark Only Light -->
  <!-- With Endorsement Light - 60px icon -->
  <!-- Size Variations Light -->
  <!-- 64px Light -->
  <!-- 44px Light -->
  <!-- 28px Light -->
  <!-- 20px Light -->
  <!-- Compact Badge Light -->
  <!-- Footer -->
  <g
     id="g24">
    <rect
       width="80"
       height="80"
       fill="#519dec"
       id="rect16"
       x="0"
       y="0" />
    <rect
       x="5.5999999"
       y="5.5999999"
       width="68.800003"
       height="68.800003"
       fill="#0256b6"
       id="rect17" />
    <!-- Horizon line -->
    <line
       x1="13"
       y1="36"
       x2="67"
       y2="36"
       stroke="#ffffff"
       stroke-width="2"
       id="line17-3" />
    <!-- Orange sun -->
    <circle
       cx="40"
       cy="36"
       r="5"
       fill="#e35205"
       id="circle17-5" />
    <!-- Arcs - alternating -->
    <path
       d="m 14,36 a 26,24 0 0 1 52,0"
       stroke="#d62598"
       stroke-width="2.2"
       fill="none"
       id="path17" />
    <path
       d="m 20,36 a 20,18 0 0 1 40,0"
       stroke="#ffffff"
       stroke-width="2.2"
       fill="none"
       opacity="0.7"
       id="path18" />
    <path
       d="m 26,36 a 14,13 0 0 1 28,0"
       stroke="#d62598"
       stroke-width="2.2"
       fill="none"
       id="path19" />
    <path
       d="m 31,36 a 9,8 0 0 1 18,0"
       stroke="#ffffff"
       stroke-width="2.2"
       fill="none"
       opacity="0.7"
       id="path20" />
    <!-- Converging lines -->
    <line
       x1="40"
       y1="41"
       x2="20"
       y2="68"
       stroke="#ffffff"
       stroke-width="2"
       stroke-linecap="round"
       opacity="0.7"
       id="line20-6" />
    <line
       x1="40"
       y1="41"
       x2="60"
       y2="68"
       stroke="#ffffff"
       stroke-width="2"
       stroke-linecap="round"
       opacity="0.7"
       id="line21-1" />
    <line
       x1="40"
       y1="41"
       x2="13"
       y2="62"
       stroke="#d62598"
       stroke-width="2"
       stroke-linecap="round"
       id="line22-7" />
    <line
       x1="40"
       y1="41"
       x2="67"
       y2="62"
       stroke="#d62598"
       stroke-width="2"
       stroke-linecap="round"
       id="line23" />
  </g>
</svg>
